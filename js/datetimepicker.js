(($) => {

  function datepicker(options) {

    return this.each((index, ele) => {
      if (this.is('input')) {
        // init
      } else {
        // init
      }
    });

  }

  $.fn.extend({ datepicker });

})(jQuery);
